# Metamodel Pointer — Viewpoint
#
# Auto-generated by Phase 1 metamodel rollout.
# Updated 2026-08-11: dea-web-viewer is now live at its own Pages route.
# The /metamodel/ route in technehub-labs.github.io still serves the
# classic class-grid navigator (visual-refinement work from PR #66) —
# that is a separate tool, not a redirect target.
#
# Canonical entity_id: dea:entity-viewpoint (kebab-singular form matching
# schemas/entities/viewpoint.json).
metamodel:
  version: v3.0.0-alpha
  entity_id: dea:entity-viewpoint
  class_alias: VP
  layer: L1
  viewer_url: "https://technehub-labs.github.io/dea-web-viewer/?entity=Viewpoint"

catalog:
  name: Viewpoints
  repo: technehub-labs/dea-web-viewer
  status: active   # was: planned. Updated 2026-08-11 when v1 shipped.

description: |
  A tailored perspective on the metamodel for a specific stakeholder concern (e.g. capability map, application landscape, technology inventory). Renders a curated subset of entities + relationships.

links:
  metamodel_repo: https://github.com/technehub-labs/dea-metamodel
  # Source of truth for the artefacts this viewer renders. Public Pages
  # repo so the dea-web-viewer sync bot can read it without auth.
  artefact_source: https://github.com/technehub-labs/technehub-labs.github.io/tree/main/metamodel
  viewer: "https://technehub-labs.github.io/dea-web-viewer/?entity=Viewpoint"
  # The classic class-grid navigator lives at /metamodel/ and is a
  # SEPARATE tool — not a redirect target.
  classic_navigator: "https://technehub-labs.github.io/metamodel/"
  github: https://github.com/technehub-labs/dea-web-viewer